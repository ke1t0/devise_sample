<h1>ツイート一覧</h1>

<% @tweets.each do |tweet| %>
  <hr>
  <h4><span>ツイート主：</span><%= link_to tweet.user.email, user_path(tweet.user.id) %></h4>
  <p><span>ツイート内容：</span><%= link_to tweet.body, tweet_path(tweet.id) %></p>
<% end %>